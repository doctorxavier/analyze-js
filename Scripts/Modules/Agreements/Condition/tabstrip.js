$(document).ready(function() {
    //INICIALIZAMOS TabStrip
    $(".tabstrip").kendoTabStrip({
        animation:  {
            open: {
                effects: "fadeIn"
            }
        }
    });
});