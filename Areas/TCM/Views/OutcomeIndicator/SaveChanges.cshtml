
<script>
    $(document).ready(function () {
        $("#lnkCancelSaveIndicatorChanges").click(function () {
            $(window.parent.document).find('body').find(".ui-widget-overlay").remove();
            $(".ui-widget-overlay").remove();
            $(".k-window").remove();
        });

        $("#btnSaveIndicatorChanges").click(function () {
            $(window.parent.document).find('body').find(".ui-widget-overlay").remove();
            $(".ui-widget-overlay").remove();
            $(".k-window").remove();
            SaveChanges();
        });
    });
</script>

@{
    var confirmMessage = Localization.GetText("TCM.RCMW.RegisterChanges.TextMessage");
}

<div style="padding: 20px;">
    @confirmMessage
</div>
<div class="pie pieReassign">
    <div class="botones">
        <a title="@Localization.GetText("TCM.RCMW.RegisterChanges.Cancel")" class="cancel" id="lnkCancelSaveIndicatorChanges" href="javascript:void(0)"> @Localization.GetText("TCM.RCMW.RegisterChanges.Cancel")</a>
        <label for="delete">
            <input type="button" value=" @Localization.GetText("TCM.RCMW.RegisterChanges.Save")" class="btn-primary" id="btnSaveIndicatorChanges">
        </label>
    </div>
</div>
