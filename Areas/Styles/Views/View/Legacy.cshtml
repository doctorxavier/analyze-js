@{
    Layout = "~/Areas/Styles/Views/Shared/_Layout_Legacy.cshtml";
}

@section JS{
    <script type="text/javascript">

        function edit() {
            enterEditMode(false, $(document), false, false, false);
        }

        function cancel() {
            exitEditMode(false, $(document), false, false, false);
        }
    </script>
}

<div id="PageContent"
     data-parsley-validate="data-parsley-validate"
     data-parsley-excluded="[disabled]">
    <a name="titulos" class="mb20">
        @Confluence.Navigation(Confluence.LabelMainTitle("Menú de la Guía de Estilos Legacy"), Url.Action("NewConfluence", "View", new { area = "Styles" }))
    </a>

    <div class="filterNormal explain">
        En el caso que su desarrollo no utilize el archivo o funciones que se encuentran en framework.js como modales con vex,
        validateContainer y parsley, favor de ver la guia de NewConfluence, puede volver con la flecha al lado del titulo del menu.
    </div>

    @Html.Partial("Partials/Menu")

    <div class="floatButtons">
        <div data-pagemode="read">
            @Confluence.ButtonBlue(Localization.GetText("OP.OD.Edit"), action: "edit")
        </div>

        <div data-pagemode="edit" class="hide">
            @Confluence.ButtonOrange(Localization.GetText("OP.OD.Cancel"), action: "cancel")
        </div>
    </div>

    @Html.Partial("Information/Information")
    @Html.Partial("Examples/Buttons")
    @Html.Partial("Examples/Labels")
    @Html.Partial("Examples/Tables")
    @Html.Partial("Examples/Inputs")
    @Html.Partial("Examples/Icons")
    @Html.Partial("Examples/Others")
</div>
