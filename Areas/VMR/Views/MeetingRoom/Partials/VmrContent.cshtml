@model IDB.MW.Application.VMRModule.ViewModels.VmrViewModel

    @if (Model.Comments != null)
    {
        @Html.Hidden("derivedBy", Model.Comments.DerivedByUserName)
        @Html.Hidden("derivedByFullName", Model.Comments.DerivedByFullName)
        @Html.Hidden("currentUserName", IDBContext.Current.UserName)
    }
    @Html.Hidden("minuteCompleteMessage", Model.BasicData.MessageMinuteDone)
    @Html.Partial("Partials/BasicData", Model.BasicData)
    @Html.Partial("Partials/TabMeetingRoom", Model)
