@model IDB.MW.Application.VERModule.ViewModels.VerViewModel

@if (Model.PolicyWaiver != null && Model.PolicyWaiver.IsVisibleTab)
{
    Model.BasicData.IsPolicyWaiverRequested = Model.PolicyWaiver.IsRequested;
}

@Html.Partial("Partials/BasicData", Model.BasicData)

@Html.Partial("Partials/TabSectionPartial", Model)
