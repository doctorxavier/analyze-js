@using IDB.Presentation.MVC4.Helpers
@using IDB.MVCControls.General.Helpers
@using IDB.Architecture.Language

@if (ViewBag.WriteRole)
{
    <div data-id="headerButtons" class="float-right main-buttons">
        <div data-pagemode="read">
            @Display.Button01(@Localization.GetText("Edit"), action: "edit")
        </div>
        <div data-pagemode="edit">
            @Display.ButtonLink(@Localization.GetText("Cancel"), action: "cancel")
            @Display.Button05(@Localization.GetText("Save"), action: "save")
        </div>
    </div>
}
