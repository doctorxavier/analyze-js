@model IDB.MW.Domain.Models.Architecture.ResultMatrix.Impacts.ImpactModel
@{
    ViewBag.Title = "New";
}

@using (Html.BeginForm("New", "Outcomes", FormMethod.Post, new { id = "outcomeEditForm"}))
{
    @Html.HiddenFor(model => model.ResultsMatrixId)
    <div class="mod_contenido_central impacts">
        <section>
            <!-- Section Header-->
            <h3 class="headingh2 tipo1"><a href="javascript:void(0)" title="Impacts detail">@Localization.GetText("Outcomes")</a></h3>
            <div class="editingButtonsEdit">
                <label for="cancel1">
                    <a href="@(Url.Action("Index", "Outcomes", new { area = "ResultsMatrix", operationNumber = ViewData["operationNumber"] }))" class="cancel link12" title="@Localization.GetText("Cancel")">@Localization.GetText("Cancel")</a>
                </label>
                <label for="save1">
                    <input type="submit" class="btn-primary_2 save" value="Save" id="save_1">
                </label>
            </div>

        </section>
        <!-- End Section Header-->
        <section>
            <!-- Section Main Content-->
            <h4 class="headingh3">@Localization.GetText("New Outcome")</h4>
            <div class="ftl w75 marginLeft4Con7">
                <fieldset>
                    <legend class="hide">@Localization.GetText("verification and observations")</legend>
                    <div class="custom">
                        <label class="editLabel input marginBottom2Con8" for="means">
                            <span class="dataTitle inputTitle">@Localization.GetText("Statement")</span>
                            @Html.TextBoxFor(model => model.Statement, new { @class = "input normal" })
                        </label>
                        <label class="editLabel input marginBottom2Con8" for="observations">
                            <span class="dataTitle inputTitle">@Localization.GetText("Observations")</span>
                            @Html.TextBoxFor(model => model.Observations, new { @class = "input normal" })
                        </label>
                    </div>
                </fieldset>
                <fieldset>
                    <legend class="hide">@Localization.GetText("verification and observations")</legend>
                    <div class="custom">
                        <label class="editLabel input marginBottom2Con8" for="observations">
                            <span class="dataTitle inputTitle">@Localization.GetText("Order Number")</span>
                            @Html.TextBoxFor(model => model.OrderNumber, new { @class = "input normal" })
                        </label>
                    </div>
                </fieldset>
            </div>
            <!--End Dropdawn-->
        </section>
        <section>
            <!-- Section Header-->
            <div class="editingButtonsEdit">
                <label for="cancel1">
                    <a href="@(Url.Action("Index", "Outcomes", new { area = "ResultsMatrix", operationNumber = ViewData["operationNumber"] }))" class="cancel link12" title="@Localization.GetText("Cancel")">@Localization.GetText("Cancel")</a>
                </label>
                <label for="save1">
                    <input type="submit" class="btn-primary_2 save" value="@Localization.GetText("Save")" id="save_2">
                </label>
            </div>

        </section>
        <!-- End Section Main Content-->
    </div>
}
