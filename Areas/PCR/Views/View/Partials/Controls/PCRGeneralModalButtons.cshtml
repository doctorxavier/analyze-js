@using IDB.Presentation.MVC4.Helpers

@if (ViewBag.SPDRole)
{
    <div data-PageMode="read">
        <button class="btn btn-primary" data-action="editGeneralModal">@Localization.GetText("PCR.Edit")</button>
    </div>
    <div data-PageMode="edit">
        <button class="btn btn-link" data-action="cancelGeneralModal">@Localization.GetText("PCR.Cancel")</button>
        <button class="btn btn-warning" data-action="saveGeneralModal">@Localization.GetText("PCR.Save")</button>
    </div>
}